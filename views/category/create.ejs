<%- include('../partials/header'); %>

<body class="container">
<%- include('../partials/aside') %>
    <main class="content">
        <h1>Création d'une nouvelle catégorie</h1>
        <form id="formCategory" action="/admin/categories/create" method="post">
            <% if(locals.error) {%>
                <div class="message is-danger">
                    <p class="message-body"><%= error.message %></p>
                </div>
            <% } %>
            <div class="field">
                <label class="label">Nom</label>
                <div class="control">
                    <input class="input" name="name" type="text" placeholder="Nom de la catégorie">
                </div>
            </div>
            <div class="field is-grouped">
                <div class="control">
                    <button type="submit" class="button is-link">Enregistrer</button>
                </div>
                <div class="control">
                    <a href="/admin/categories" class="button is-link is-light">Annuler</a>
                </div>
            </div>
        </form>
    </main>
</body>
</html>
